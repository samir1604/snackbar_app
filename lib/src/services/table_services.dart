import 'package:rxdart/rxdart.dart';


class TableServices {
  /*
  final Map<int, Table> _tables = {};
  final BehaviorSubject<Table?> _notificationSubject =
      BehaviorSubject.seeded(null);

  ValueStream<Table?> get table$ => _notificationSubject.stream;

  void addTable(Table table) => _tables[table.id] = table;

  void selectTable(int value) => _notificationSubject.add(_tables[value]);

  void updateTable(Table table) {
    _tables[table.id] = table;
    _notificationSubject.add(table);
  }

   */
}
